import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personallifemanager.app',
  appName: 'Personal Life Manager',
  webDir: 'dist'
};

export default config;
